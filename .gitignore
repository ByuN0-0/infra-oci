# 테라폼 로컬 작업 디렉토리 (프로바이더, 모듈 등 설치됨)
.terraform/

# 테라폼 상태 파일 (Terraform Cloud를 사용하므로 로컬에는 없어야 하지만 보안상 추가)
*.tfstate
*.tfstate.*

# 테라폼 변수 파일 (비밀키, OCID 등 민감 정보 포함 가능) - 매우 중요!
*.tfvars
*.tfvars.json

# 로그 파일
*.log
crash.log

# 로컬 설정/재정의 파일
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# .terraformrc 혹은 terraform.rc
.terraformrc
terraform.rc

# OS 관련 파일
.DS_Store
Thumbs.db